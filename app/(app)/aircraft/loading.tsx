import LoadingPanel from "../loading-panel";

// The fleet panel: a list of aircraft cards with a form beside it.
export default function Loading() {
  return <LoadingPanel label="your aircraft" shape="panel" />;
}
